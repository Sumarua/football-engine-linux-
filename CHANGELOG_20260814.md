# 变更日志 / 提交说明（2026-08-14 优化批次）

> 本次为「重新检测 + 深挖 + 修复」批次，对应 commit `c21d5a3` 及其后补丁。
> 核心结论：模型自身弱于市场（Brier 0.69 vs 市场 0.63），因此所有"价值/串关/让球"
> 的 EV 计算必须**以市场为主导**，绝不能再"赌模型比市场懂"。

## 一、深挖结论（方法论）

### 1. 去水方法：Shin = 乘法归一化，幂法更差
- 185 场结算样本实测：**乘法归一化 Brier 0.6351 = Shin 0.6351 < 幂法 0.6410**
- 竞彩 1X2 的 Shin 参数 z→0（无内幕交易偏斜），退化为乘法归一化
- 幂法 k>1 会把热门概率抬高/冷门压低（假设"无热门-冷门偏差"），但竞彩 1X2 实际
  不存在该偏差，反而更差
- **结论：维持 Shin 去水即可，无需改。**

### 2. 水位（赔率变动）信号方向反了（真 bug）
- fetch 口径：`compression = 初盘/即时盘`，>1 = 赔率下降 = 资金涌入
- main.py 却写成"<0.95 = 下降"，符号反转 → 盘口信号被反向应用
- 已修复：>1.05 加仓、<0.95 减仓（与 fetch 口径对齐）

### 3. 让球（亚洲盘）是独立预测，但不能对抗市场
- 让球方向与胜平负方向可以不同（本来就是独立玩法），保留
- 但让球 EV 之前用纯模型比分矩阵（Brier 弱于随机），回测 83 场 ROI -18.1%
- 已改：让球概率 = (1-w)×模型 + w×市场去水，w=0.882（与 1X2 融合同口径）
  + 方向一致性闸（模型让球方向必须与市场同向才推荐）

## 二、修复清单

### 数据层（P0）
| 问题 | 修复 |
|---|---|
| 波胆/总进球官方赔率被 `f` 标志键清零（比分串/总进球 EV 静默全灭） | `parse_crs_odds/parse_ttg_odds` 跳过 `f` 键 |
| 新浪抓取用 UTC 选日期（北京 0-8 点抓错，8/14 sina_odds 全缺） | 改北京时间 |
| 水位监控写键=新浪ID / 读键=竞彩编号（永不命中） | 读取端按 match_no+开赛日期反查，拒绝跨周 |
| selfcheck 只告警不阻断 | 加 `--fail-on-critical`（关键字段全缺退出码 2） |

### 展示层（P0/P1）
| 问题 | 修复 |
|---|---|
| 未开赛比赛显示伪造赛果（裸场次号跨日期匹配） | 只按 match_id/队名匹配，未来日期不 fallback |
| "近7天"硬编码 2026-07-30 | 滚动窗口 |
| 系统面板融合权重写死"模型60/市场25" | 读实际状态（模型10/市场75） |
| 预期回报"0.13x"红色误导 | 显示 +13.2% 绿色 |
| 赔率变动只显示压缩比 | 改为 初盘→即时盘 对比（主2.63→2.35↓） |

### 模型层（P1）
| 问题 | 修复 |
|---|---|
| `direction_prob` 取 max(H,D,A)，R1 改判平局时=主胜概率 → 串关 EV 虚高 | 取所选方向自身概率 |
| 串关选腿绕过送钱区禁投（选芬超平局） | 串关腿过 league_forbid |
| 合成赔率出注（数学必亏 4.8%，曾押 ¥67,353） | 一律不出注 |
| accuracy_trend Brier 缺失按 0 计（market 0.4502 假值） | 只对有值行求均值（market 0.629） |
| 融合权重越界静默运行（model 0.10<0.15、market 0.75>0.6） | get_champion 读盘强制边界 + 放宽边界 |
| 让球 EV 对抗市场 | 市场主导融合 + 方向一致闸 |

### 卫生（P2/P3）
- `.wrangler/` 账号 PII + `.bak` 备份移出 git
- auto_run.sh 重试加 `pull --rebase`、去掉 `engine/` 提交、venv 探测
- workflow selfcheck 预测后阻断关键全缺

## 三、关于"串关/让球/水位"的定位（按你的要求）

1. **串关保留**：方向概率修正后 cal_ev 诚实，`cal_ev>0` 即标推荐；market_ev 仍落盘
   展示（几乎必然为负 = 双重抽水，供参考），不做硬否决。
2. **让球保留**：作为独立预测（方向可与胜平负不同）；但下注用市场主导融合概率，
   只有"模型与市场同向"才推荐——避免再出现"模型 51% vs 市场 40%"的对抗单。
3. **水位监控**：重点是**变化对比（初盘→即时盘）+ 核算（信号命中率）**，已修信号
   方向反了的 bug，页面改显初盘→即时盘对比。

## 四、遗留（下一步可做）
- 让球概率的模型源（比分矩阵）本身 Brier 弱于随机，融合只是"压制"而非"修复"——
  根本解法是校准比分矩阵或换概率源（如直接用市场去水 + 微小模型扰动）。
- 送钱区/解禁判定样本仍小（n=10-24），建议 n≥50 才翻转 verdict。
- 冠军选择在 n=20 验证窗做 0.002-0.005 Brier 判定 = 噪声，建议窗口≥100 + bootstrap。
